export const CJ_PRICE_MULTIPLIER = 5; export const MANUAL_PRODUCT_PREFIX = 'MANUAL-'; export function isManualProductId(aliexpressId?: string | null) { return Boolean(aliexpressId && aliexpressId.startsWith(MANUAL_PRODUCT_PREFIX)); } export function getDisplayedCjPrice(basePrice: unknown, aliexpressId?: string | null) { return isManualProductId(aliexpressId) ? Number(basePrice) : Math.round(Number(basePrice) * CJ_PRICE_MULTIPLIER * 100) / 100; }